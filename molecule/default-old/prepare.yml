---
- name: Prepare
  hosts: all

  tasks:
  - name: Ensure python requirements installed.
    apt:
      name:
        - python-pip
        - python-setuptools
        - python3-pip
        - python3-setuptools
      state: present
    become: true
